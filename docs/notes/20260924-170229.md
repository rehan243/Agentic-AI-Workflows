## Note on agent orchestration

A recurring lesson working on tool routing, planning loops, and guardrails: the failure mode is almost never where the intuition points. Measuring the change end-to-end — not reasoning about it in isolation — is what surfaces the real bottleneck. Documenting it here so the next decision starts from evidence rather than a guess.
